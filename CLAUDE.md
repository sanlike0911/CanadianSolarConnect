# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

本プロジェクトの指示書は、Codex 等の他の AI エージェントと共通の単一ソースとして `AGENTS.md` に集約している。以下にその内容を取り込む。`AGENTS.md` の指示に従うこと（指示書を更新する際は `AGENTS.md` 側を編集する）。

@AGENTS.md
